/** Scorecard dimension labels for inline tooltips (matches server prompt semantics). */
export const SCORECARD_DIMENSION_LABELS = {
  FI: 'Feature importance (0–4)',
  R: 'Reach / adoption (0–3)',
  TS: 'Technical severity (0–3)',
  WQ: 'Workaround quality (0–2)',
  RR: 'Regression risk (0–2)',
};

/** Long names used in Scorecard metrics grid (matches Backlog metrics helper text). */
export const SCORECARD_DIMENSION_LONG = {
  feature_importance: 'Feature importance',
  reach: 'Reach',
  technical_severity: 'Technical severity',
  workaround_quality: 'Workaround quality',
  regression_risk: 'Regression risk',
};
